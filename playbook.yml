site:
  title: BlueRange Fruitymesh
  # replace this with site url when available
  url: http://localhost:5000
  start_page: fruitymesh::README.adoc
content:
  sources:
    - url: https://github.com/PanaDel/fruitymesh.git
      start_path: docs
      branches: master
ui:
  bundle:
    # when testing locally uncomment folowing url
    # url: https://repo.mwaysolutions.com/relution/bluerange/antora-docs/ui-bundle/-/jobs/artifacts/master/raw/build/ui-bundle.zip?job=bundle-stable
    # and comment the one bellow
    url: ./ui-bundle.zip
    snapshot: true
runtime:
  fetch: true
output:
  clean: true
  dir: ./docs/site
